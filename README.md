
##    Реализация по задаче сервиса котировок.
    
    Хочу от себя добавить несколько слов по реализации.
    В стеке видны ограничения. Java 8. К сожалению я отошел немного от правил и наисал сервис на Java 21.
    Еще была идея отойти от правил. Вместо MVC использовать WebFlux, мне казалось, что это ускорит взаимодействие клиента с сервисом.
    Но решил сделать иначе, добавил в сервис работу с kafka.
    Для быстрой обработки входящих запросов (100tps), мой сервис кладет сообщения в кафка и тем самым, достигает быстрый прием заросов.
    Далее добавлен слушатель с возможностью обработки сообщений используя многопоточку.
    В ТЗ мало информации, о том, нужно ли клиенту что то знать, как прошла обработка. подумал, что не стоит, так как котировки могут меняться мгновенно.
    Понятно, что это тестовое ТЗ. Поэтому можно придумать что то свое. Например, добавить возможность включения/отключения кафки. 
    Даже на лету. не перезапуская инстансы. Да можно тут наворотить)))
    Была идея использовать NoSQL, но не стал это делать, так как не было четких требований) Хотя мне кажется было бы неплохо) Все зависит от кейсов)
    Покрыл юнит тестами. Но можно и прикрутить postgres test-containers.
    Добавил сваггер для удобства тестирования. и для клиентов сервиса
    Если Вас заинтересовал мой код, то буду готов с удовольствием отвечать на Ваши вопросы.

####    Для локального запуска запустите Docker.
####    выполните запуск docker/docker-compose.yml
####    Для запуска приложения выполните gradle bootRun

####    После запуска можно использовать Swagger
####    http://localhost:8080/swagger-ui/index.html#/

# ТЗ
    
    Написать сервис для сохранения котировок и получения elvl по
    инструментам (бумагам) по REST API.
    Функциональные требования:
    • Сервис получает котировки
    • Сервис хранит историю полученных котировок в БД
    • Сервис рассчитывает elvl (правила ниже)
    • Сервис предоставляет elvl по isin
    • Сервис предоставляет перечень всех elvls
    Предусмотреть обработку большого потока котировок (100 в секунду) по нескольким бумагам, в том числе и
    одновременную обработку нескольких котировок по одной бумаге.
    Сущности:
    • Котировка (quote) состоит из следующих полей: isin, bid, ask
    • elvl (energy level) – лучшая цена по данному инструменту (isin).
    Правила расчёта elvl на основе котировки:
    1. Если bid > elvl, то elvl = bid
       2. Если ask < elvl, то elvl = ask
       3. Если значение elvl для данной бумаги отсутствует, то elvl = bid
       4. Если bid отсутствует, то elvl = ask
          Реализовать простую валидацию входных данных для котировки:
          • bid должен быть меньше ask
          • isin – 12 символов
          Пример:
       1. ISIN: RU000A0JX0J2
          Bid: 100.2
          Ask: 101.9
          Котировка новая, поэтому elvl - > 100.2
       2. Пришла новая котировка, уже существует elvl
          ISIN: RU000A0JX0J2
          Bid: 100.5
          Ask: 101.9
          Так как Bid > elvl , то новый elvl = 100.5 (bid)
          Стек технологий:
          • Kotlin/Java8
          • Maven/Gradle
          • Spring boot
          • Spring MVC, Spring Test
          С помощью тестов продемонстрируйте, что функционал работает так, как ожидается
